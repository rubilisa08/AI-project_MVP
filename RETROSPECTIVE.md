# 회고록 — AI Business Briefing & Intelligence Publisher

> 아래는 git 커밋 히스토리(2026-09-11 ~ 09-13)를 바탕으로 잡은 초안입니다.
> 실제로 느꼈던 감정·디테일을 더해 본인 말투로 다듬어서 제출하세요.

## 프로젝트를 시작하며

3일(9/10~9/12) 동안 "기업 공시·재무제표·뉴스를 읽고 C-Level 브리핑을 만드는" 문제를
Multi-Agent 파이프라인으로 풀어보고 싶었습니다. 단순히 "AI에게 요약을 시킨다"가 아니라,
**AI가 만든 결과물을 어떻게 사람이 믿을 수 있게 만들 것인가**가 이번 프로젝트의 진짜 주제였습니다.

이번 프로젝트에서 제가 가장 중점을 둔 부분은 **자료에 제시된 숫자 하나하나의 정확성**이었습니다.
재무 수치는 틀리면 바로 잘못된 의사결정으로 이어질 수 있는 영역이라, 속도나 기능 다양성보다
"사람이 보기에도 흠 잡을 데 없이 신뢰할 수 있는가"가 가장 중요하다고 생각했습니다. 다소 시간이
걸리더라도 무조건 정확한 결과만 내놓아야 하고, 정확히 분석할 수 없다면 차라리 결과를 보여주지
않는 편이 낫다는 원칙(보안도 마찬가지)으로 설계와 검증 모두를 밀어붙였습니다.

## AI와 협업하며 배운 점

- **"LLM에게 계산을 시키지 않는다"는 설계가 핵심이었다.** 재무비율(부채비율, ROE 등)을
  LLM이 텍스트로 답하게 하면 종종 미묘하게 틀린 값을 냈습니다. Claude의 Code Execution
  Tool로 실제 pandas 코드를 실행시키고, 그 결과를 TypeScript로 짠 별도 계산과 대조하는
  **이중 계산 검증** 구조를 만들고 나서야 "숫자를 믿을 수 있다"고 말할 수 있는 수준이
  됐습니다. AI를 신뢰하는 게 아니라, AI의 출력을 검증하는 구조를 AI와 함께 설계한 과정이
  인상 깊었습니다.
- **환각 방지는 사후 필터링이 아니라 파이프라인 단계별 책임 분리로 풀어야 했다.**
  Collector는 LLM을 아예 호출하지 않고 결정론적 파싱만 하게 하고, Fact-Checker는
  "인용한 청크 id가 실제로 존재하는가"를 코드로 먼저 거르고 나서야 LLM에게 원문 대조를
  맡겼습니다. 한 번에 "환각 없이 써줘"라고 프롬프트로 요청하는 것보다, 역할을 쪼개서
  서로 검증하게 만드는 편이 훨씬 안정적이었습니다.
- **PRD를 실제 구현과 계속 대조하며 갱신하는 습관이 유용했다.** 처음 PRD에 LangChain/
  LangGraph, Claude 3.5 Sonnet & Haiku 이원화를 적어뒀지만, 실제로는 더 가벼운 커스텀
  오케스트레이션과 단일 모델로도 충분했습니다. AI와 함께 "PRD 대비 실제 구현 현황"
  섹션을 만들어 차이를 투명하게 기록해둔 것이 스스로도, 나중에 다시 봤을 때도 도움이
  됐습니다.

## 어려웠던 점

- **AI를 활용하면 전체적인 틀은 빠르게 만들어지지만, 디테일한 부분에서 막상 동작하지
  않는 경우가 생각보다 많았습니다.** 파이프라인 구조나 화면은 금방 완성됐는데, 실제로
  파일을 올려보면 특정 케이스에서만 조용히 실패하거나 값이 이상하게 나오는 일이 잦았고,
  "왜 안 되는지"를 찾는 디버깅 과정이 오히려 처음 기능을 만드는 것보다 더 까다롭고
  시간이 오래 걸렸습니다. 대표적으로 로컬에서는 멀쩡한데 배포 환경에서만
  `/api/analyze`가 매번 500 에러를 내는 문제가 있었는데, 원인이 PDF 파싱 모듈을
  요청 시점이 아니라 모듈 로드 시점에 초기화하던 것이었다는 걸 찾아내기까지 꽤
  헤맸습니다. AI가 짜준 코드를 그대로 믿기보다 직접 재현하고 원인을 좁혀가는 과정이
  꼭 필요하다는 걸 체감했습니다.
- **일부 핵심 기능이 유료 API 사용량에 묶여 있어서, 실제 환경 그대로 끝까지 검증하기
  어려웠던 점이 아쉬웠습니다.** Claude의 Code Execution Tool(pandas 실행, PDF 표
  추출)은 실제 키로 과금이 발생하는 구간이라, 개발·테스트 단계에서는 `MOCK_LLM` 모드로
  임계값 기반 목업 응답을 만들어 대신 검증할 수밖에 없었습니다. 실제로 전자공시시스템
  (DART)에서 임의의 회사 분기보고서를 받아 업로드해보고 실제 결과물을 확인해보고
  싶었지만, 유료 과금 때문에 이 시나리오를 끝까지 직접 돌려보며 검증하기는 어려웠습니다.
  덕분에 구조나 UI 흐름은 비용 없이 확인할 수 있었지만, 실제 LLM 판단이 들어가는 부분
  (리스크 서술, 팩트체크 판정 등)의 품질은 목업으로는 완전히 검증되지 않는다는 한계가
  남았고, 이 부분은 계속 아쉬움으로 남습니다.
- **AI에게 계속 되묻고 다시 설명해야 하는 과정 자체가 까다로웠습니다.** 한 번에
  의도한 대로 나오지 않아서 같은 부분을 여러 번 다른 방식으로 되물어야 했는데,
  돌이켜보면 그렇게 반복해서 되묻던 지점들이 곧 제가 요구사항을 스스로도 명확히
  정리하지 못하고 있던 부분이었습니다. AI와의 대화가 매끄럽지 않았던 구간은 대부분
  제 쪽 설계나 우선순위가 덜 정리된 구간이었다는 걸 나중에야 깨달았습니다.
- **"완벽하게 정확한 시스템"과 "짧은 시일 안에 끝낼 수 있는 범위" 사이의 균형**이
  계속 고민이었습니다. 예를 들어 DART 자동 크롤링은 포기하고 "링크를 드롭존에 끌어다
  놓으면 서버가 대신 받아온다"는 절충안으로 좁혔습니다.
- **AI가 만든 코드의 안전성을 직접 검토해야 하는 부분**(SSRF 방지, 파일 크기 제한 등)은
  AI 제안을 그대로 쓰기보다 실제 위협 시나리오를 하나씩 따져보며 다시 확인해야 했습니다.
- **"숫자 정확성"을 가장 중요하게 여겼는데도, 정작 검증 로직 자체에 구멍이 있었습니다.**
  프로젝트를 다시 점검하며 코드를 직접 뜯어본 결과, 두 가지를 발견했습니다. (1) 재무제표
  항등식(자산총계 = 부채총계 + 자본총계) 검증이 "TypeScript와 Python 이중 계산"이라는
  원칙과 달리 실제로는 교차 대조 없이 Python 쪽 결과로 그냥 덮어써지고 있었고, (2) 비율
  이중 검증의 "일치" 판정 로직이 한쪽 엔진에서만 값이 나온 경우(계산 실패 등)를 놓쳐서,
  일부만 검증됐는데도 "✓ 이중 검증 완료" 배지가 뜰 수 있는 상태였습니다. 둘 다 "정확히
  검증되지 않았으면 확신에 찬 결과를 보여주지 않는다"는 제 원칙에 정면으로 어긋나는
  부분이라 즉시 고쳤습니다 — 이제는 두 엔진이 일치하지 않거나 한쪽만 값을 냈을 경우
  보수적으로 "확인 필요/불일치"로 표시되도록 바꿨습니다. **"정확성이 가장 중요하다"고
  말하는 것과, 그걸 코드 구석구석까지 실제로 강제하는 것은 다른 문제**라는 걸 배웠습니다.

## 추후에 해결해야 할 목록

- 실제 DART API 연동으로 기업명만 입력해도 자동 수집되도록 확장
- 저장된 리포트를 팀과 공유/코멘트할 수 있는 기능
- Code Execution Tool 비용을 고려한 캐싱(같은 재무제표를 반복 계산하지 않도록)
